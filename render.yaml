services:
  - type: web
    name: kali-terminal
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: TERMINAL_API_KEY
        generateValue: true
      - key: NODE_ENV
        value: production
